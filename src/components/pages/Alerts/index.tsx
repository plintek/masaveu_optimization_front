import React, { ReactElement } from "react";

import MainContainer from "@components/MainContainer";

function Alerts(): ReactElement {
    return <MainContainer>Alerts</MainContainer>;
}

export default Alerts;
